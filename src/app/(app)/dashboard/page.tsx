import { DashboardContent } from "./dashboard-content";

export const metadata = {
  title: "Dashboard — mdcolab",
};

export default function DashboardPage() {
  return <DashboardContent />;
}
