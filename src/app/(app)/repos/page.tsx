import { ReposListContent } from "./repos-list-content";

export const metadata = {
  title: "Repositories — mdcolab",
};

export default function ReposPage() {
  return <ReposListContent />;
}
